# VULA GROUP — MASTER PLAN
## The Complete Business, Naming System & Claude Code Build Prompt
### Version 1.0 — May 2026

---

# PART 1: THE BRAND ARCHITECTURE

## Why "Vula"

Vula (isiZulu/isiXhosa) means **"open" / "unlock"**.
It works in every official SA language context, is one syllable, globally pronounceable,
and carries the exact meaning of what the business does — unlocks capability, intelligence,
and opportunity for businesses and people.

**The brand family: Vula Group**
Everything lives under one roof. One story. One name people remember.

---

## Naming System — Every Product, Renamed

### THE COMPANY
| Old Name | New Name | What It Is |
|---|---|---|
| Aweh Be Lekker (Pty) Ltd | **Vula Group (Pty) Ltd** | The holding company |
| — | **"by Vula"** | The mark on all products |

> Keep "Aweh Be Lekker" alive as a brand/lifestyle imprint for Awake SA consumer
> marketing. It stays on surf content, events, and community. Not on B2B products.

---

### PRODUCT 1: B2B AI Platform (was CogniCore)
| Old | New | Rationale |
|---|---|---|
| CogniCore | **Vula Business** | Simple. Direct. "Vula Business — AI that knows your business." |
| CogniCore Starter | **Vula Starter** | |
| CogniCore Growth | **Vula Growth** | |
| CogniCore Business | **Vula Pro** | |
| CogniCore Box / Soul Box | **Vula Box** | Hardware appliance — "Plug in. Power on." |
| Document Intelligence | **Vula Docs** | The ingestion + RAG feature |
| Web Intelligence | **Vula Scout** | The scraper/research feature |
| AI Quote Builder | **Vula Draft** | The proposal/quote generation feature |

**Tagline:** *Unlock your business.*
**URL:** vula.co.za / vula.ai (check availability)

---

### PRODUCT 2: Local AI Ecosystem (was Universal Soul AI / TwoSoul)
| Old | New | Rationale |
|---|---|---|
| Universal Soul AI Unlimited | **Vula Mind** | "Your AI. Your devices. Your rules." |
| TwoSoul Mobile | **Vula Mobile** | The thin client app |
| ThinKMesh | **Vula Mesh** | The parallel reasoning / device mesh engine |
| HRM (Hierarchical Reasoning Model) | **Vula Core** | The orchestration brain |
| Skill Registry | **Vula Skills** | Community skill ecosystem |
| Reflection Agent | **Vula Learn** | The self-improvement loop |

**Tagline:** *AI that lives on your devices.*
**URL:** mind.vula.co.za

---

### PRODUCT 3: Watersports Platform (was FlowCrew)
| Old | New | Rationale |
|---|---|---|
| FlowCrew | **Vula Wave** | "Unlock the ocean." — clean, on-brand |
| Beach Intelligence | **Vula Conditions** | The AI weather/conditions feature |
| Booking Engine | **Vula Book** | |
| Camera AI | **Vula Eye** | The YOLO beach camera analysis |
| Coach Marketplace | **Vula Coaches** | |
| Gamification | **Vula Rides** | Session tracking / leaderboards |

**Tagline:** *Unlock the ocean.*
**URL:** wave.vula.co.za

---

### PRODUCT 4: Autonomous Trading (was PROMETHEUS)
| Old | New | Rationale |
|---|---|---|
| PROMETHEUS | **Vula Trade** | Cleaner, no mythology baggage |
| ThinkMesh (trading context) | **Vula Mesh** | Same engine, shared name |
| Observation Layer | **Vula Watch** | Performance monitoring |
| Meta-Agent / Self-Improvement | **Vula Evolve** | The learning loop |
| Strategy Archive | **Vula Vault** | Regime-tagged strategy store |

**Tagline:** *Intelligent capital, locally sovereign.*
**URL:** trade.vula.co.za (private — not public-facing)

---

### PRODUCT 5: E-commerce (was Awake SA store / Kelp Board Bags / Medusa)
| Old | New | Rationale |
|---|---|---|
| Awake SA Store | **Awake SA** (keep the brand) | Premium brand stays premium |
| Kelp Board Bags store | **Kelp** (keep) | Lifestyle brand stays |
| Commerce Platform / Medusa template | **Vula Commerce** | The B2B white-label store builder |
| Multi-tenant admin | **Vula Admin** | Master control panel |

---

### SUPPORTING TOOLS (keep or rename)
| Old | New | Decision |
|---|---|---|
| Bass Fishin' Pro | **Bass Fishin' Pro** | Keep — it's its own brand, has users |
| Echium Park | **Echium Park** | Community project — keep name |
| DIGG | **DIGG** | Professional practice — keep |
| ITEC Group role | **ITEC** | Employer — keep |

---

# PART 2: THE BUSINESS ARCHITECTURE

## The Vula Group Map

```
VULA GROUP (Pty) Ltd — Cape Town
│
├── VULA BUSINESS (B2B AI SaaS)
│   ├── Vula Starter    — R1,500/mo  — 1-2 users
│   ├── Vula Growth     — R3,500/mo  — up to 5 users
│   ├── Vula Pro        — R7,500/mo  — unlimited users
│   ├── Vula Box        — R24,500+   — hardware appliance (one-time)
│   └── Vula Docs/Scout/Draft — features within each tier
│
├── VULA MIND (Local AI Ecosystem — open source + premium)
│   ├── Vula Mobile     — free app (Android + iOS)
│   ├── Vula Mesh       — device orchestration (self-hosted)
│   ├── Vula Core       — HRM orchestration engine (open source)
│   └── Vula Skills     — community skill marketplace
│
├── VULA WAVE (Watersports Platform)
│   ├── Consumer app    — free, users book sessions
│   ├── Operator SaaS   — R1,200/mo per beach/school
│   └── Transaction fee — 5% per booking
│
├── VULA TRADE (Private — Autonomous Trading)
│   └── Internal capital management only
│
├── VULA COMMERCE (White-label E-commerce)
│   ├── Awake SA        — internal client #1
│   ├── Kelp            — internal client #2
│   └── External clients via Vula Business upsell
│
└── LIFESTYLE / COMMUNITY
    ├── Awake SA        — eFoil/surfboard import & distribution
    ├── Bass Fishin' Pro — consumer game (PWA)
    └── Echium Park     — community signage & park project
```

## Revenue Model Summary

| Stream | Vehicle | Type | Year 1 Target |
|---|---|---|---|
| B2B SaaS | Vula Business | Monthly recurring | R600k |
| Hardware | Vula Box | One-time + support | R400k |
| Watersports | Vula Wave | Recurring + transaction | R180k |
| E-commerce | Vula Commerce | Monthly recurring | R120k |
| Architecture | DIGG fees | Project-based | R800k |
| eFoil distribution | Awake SA | Product margin | R600k |
| Managed Print | ITEC commissions | Commission | R300k |
| **Total Y1** | | | **~R3M** |

---

# PART 3: THE TECH STACK (UNIFIED)

## Infrastructure Layer (shared across all products)

```
Local Inference (X299 Rig — i9-7960X + RTX 3090 24GB)
├── Ollama serving:
│   ├── deepseek-r1:7b      — standard reasoning (Vula Core)
│   ├── deepseek-r1:14b     — complex tasks (Vula Core)
│   ├── deepseek-r1:1.5b    — routing/triage (Vula Core)
│   ├── bge-m3              — embeddings (multilingual, Afrikaans)
│   └── glm-ocr             — document OCR
│
├── LiteLLM Gateway (port 4000)
│   └── Single OpenAI-compatible endpoint for ALL products
│       Fallback chain: local → Kimi K2.6 → Claude Sonnet
│
├── Qdrant (port 6333)     — vector store (per-tenant collections)
├── PostgreSQL (port 5432) — structured data + Supabase hosted
├── Redis (port 6379)      — caching + Celery task queue
└── n8n (port 5678)        — workflow automation
```

## Product Tech Stacks

| Product | Frontend | Backend | Mobile | DB |
|---|---|---|---|---|
| Vula Business | Next.js 15 + Tailwind + shadcn | FastAPI + Python 3.12 | — | Supabase (PostgreSQL) |
| Vula Mind | React (web dashboard) | FastAPI + Python 3.12 | React Native + Expo | SQLite (local) + Qdrant |
| Vula Wave | Next.js 15 | FastAPI | React Native + Expo | PostgreSQL + PostGIS |
| Vula Trade | React dashboard | FastAPI + LangGraph | — | PostgreSQL + TimescaleDB |
| Vula Commerce | Next.js 15 | Medusa v2 | — | PostgreSQL |
| Awake SA | Next.js 15 (storefront) | Medusa v2 | — | PostgreSQL |

## Payments
- **ZAR billing:** PayFast (subscriptions + one-time)
- **International:** Stripe
- **Crypto (future):** Algorand / USDC

## Hosting
- **API servers:** Railway (auto-deploy from GitHub)
- **Frontends:** Vercel
- **Local inference:** X299 rig on home network
- **Domain:** vula.co.za + vula.ai

---

# PART 4: THE CLAUDE CODE BUILD PROMPT

Copy everything below this line into a new Claude Code session.

---

```
════════════════════════════════════════════════════════════════
CLAUDE CODE — VULA GROUP MASTER BUILD PROMPT
Version 1.0 | May 2026
════════════════════════════════════════════════════════════════

You are building the Vula Group software platform — a suite of AI
products built in Cape Town, South Africa by Aweh Be Lekker (Pty)
Ltd trading as Vula Group.

READ EVERYTHING IN THIS PROMPT BEFORE WRITING A SINGLE LINE OF CODE.
This context is non-negotiable and applies to every file you create.

────────────────────────────────────────────────────────────────
SECTION 1: WHO WE ARE
────────────────────────────────────────────────────────────────

Vula Group is a Cape Town-based AI technology company. "Vula" means
"unlock" in isiZulu/isiXhosa — our mission is to unlock intelligence
and capability for South African businesses and individuals.

The two principals are:
- Judy Downing — SACAP-registered architect (PAT44740093), director
  of DIGG (property development and architecture practice)
- Richard Downing — business development, Awake SA eFoil distributor,
  ITEC managed print services

Our unfair advantages:
1. Local-first AI (runs on our hardware, zero per-token cost at scale)
2. POPIA-compliant by architecture (data stays in SA)
3. ZAR-native pricing via PayFast
4. Existing B2B relationships via ITEC and DIGG
5. DeepSeek R1 think-trace interception via Vula Mesh (unique)
6. Device mesh networking (no competitor has this)

────────────────────────────────────────────────────────────────
SECTION 2: THE PRODUCTS TO BUILD
────────────────────────────────────────────────────────────────

BUILD PRIORITY ORDER — build these in sequence, one at a time:

## PRIORITY 1: VULA BUSINESS (B2B AI SaaS) — BUILD FIRST
The revenue engine. Gets us to R10,500/month MRR with 3 clients.

### What it does
Vula Business is a multi-tenant AI platform that learns each client's
business (from their uploaded documents) and acts as an intelligent
team member — drafting quotes, answering staff questions, researching
leads, monitoring tenders, generating reports.

### Tech stack
- Frontend: Next.js 15, TypeScript, Tailwind CSS, shadcn/ui
- Backend: FastAPI (Python 3.12), async throughout
- Database: Supabase (PostgreSQL with RLS per tenant)
- Auth: Supabase Auth (email + Google OAuth)
- Payments: PayFast subscriptions (ZAR)
- AI layer: LiteLLM gateway → local Ollama (DeepSeek R1 7B/14B)
- Vector store: Qdrant (one collection per tenant: "vula_{tenant_id}")
- Document OCR: GLM-OCR via Ollama, fallback to pdfplumber
- Embeddings: BGE-M3 via Ollama (multilingual, works with Afrikaans)
- Notifications: WhatsApp Business API + email (Resend)
- Hosting: Railway (backend) + Vercel (frontend)
- Domain: vula.co.za

### File structure to create
```
vula-business/
├── backend/
│   ├── main.py                          # FastAPI app entry point
│   ├── api/
│   │   ├── auth.py                      # Supabase auth middleware
│   │   ├── tenants.py                   # Tenant CRUD
│   │   ├── onboarding.py               # Signup + provisioning
│   │   ├── documents.py                # Upload + ingestion endpoints
│   │   ├── query.py                    # RAG query endpoint
│   │   ├── scraper.py                  # Web intelligence endpoints
│   │   └── admin.py                    # Master admin endpoints
│   ├── ingestion/
│   │   ├── pipeline.py                 # Full ingestion pipeline
│   │   ├── ocr.py                      # GLM-OCR + fallbacks
│   │   ├── chunker.py                  # Semantic chunker
│   │   └── embedder.py                 # BGE-M3 embeddings
│   ├── intelligence/
│   │   ├── rag.py                      # RAG answer generation
│   │   ├── scraper.py                  # Crawl4AI + ScrapeGraphAI
│   │   └── tender_monitor.py          # SA government tender tracking
│   ├── billing/
│   │   ├── payfast.py                  # PayFast subscription logic
│   │   └── webhooks.py                 # PayFast IPN webhooks
│   ├── notifications/
│   │   ├── whatsapp.py                 # WhatsApp Business API
│   │   └── email.py                    # Resend email
│   ├── models/
│   │   └── schemas.py                  # Pydantic models
│   ├── db/
│   │   ├── supabase.py                 # Supabase client
│   │   └── migrations/
│   │       └── 001_initial.sql         # All table definitions
│   └── requirements.txt
│
├── frontend/
│   ├── app/
│   │   ├── page.tsx                    # Landing/marketing page
│   │   ├── pricing/page.tsx            # Pricing page (3 tiers)
│   │   ├── onboarding/page.tsx         # 5-step onboarding flow
│   │   ├── dashboard/
│   │   │   ├── page.tsx                # Main client dashboard
│   │   │   ├── docs/page.tsx           # Document management
│   │   │   ├── ask/page.tsx            # AI query interface
│   │   │   ├── scout/page.tsx          # Web intelligence
│   │   │   └── settings/page.tsx       # Tenant settings
│   │   └── admin/
│   │       ├── page.tsx                # Master admin (Richard/Judy)
│   │       └── clients/page.tsx        # Client list + status
│   ├── components/
│   │   ├── ui/                         # shadcn/ui components
│   │   ├── UploadZone.tsx              # Drag-drop document upload
│   │   ├── QueryPanel.tsx              # Ask AI interface
│   │   ├── ScoutPanel.tsx              # Web intelligence tabs
│   │   ├── DocumentList.tsx            # Ingested documents
│   │   └── TierCard.tsx               # Pricing tier display
│   ├── lib/
│   │   ├── supabase.ts                 # Supabase client
│   │   ├── api.ts                      # API client functions
│   │   └── types.ts                    # TypeScript types
│   ├── package.json
│   ├── tailwind.config.ts
│   └── tsconfig.json
│
└── docker-compose.yml                   # Local dev: Qdrant + Redis + n8n

### Key business rules to enforce in code
1. All tenant data isolated via tenant_id in Supabase RLS
2. Qdrant collection per tenant: f"vula_{tenant_id.replace('-','_')}"
3. All prices in ZAR cents (no decimals in DB)
4. POPIA compliance: no data leaves SA unless tenant explicitly opts in
5. WhatsApp notification to Richard (+27...) on every new signup
6. 30-day free trial before any billing
7. PayFast subscription created on day 28, deducts on day 31
8. Documents stored in Supabase Storage bucket: f"tenant-{tenant_id}"
9. Ingestion is async (background task) — never block the upload response
10. All LLM calls go through LiteLLM gateway at http://localhost:4000

### Pricing tiers (enforce in billing code)
- vula_starter: 150000 ZAR cents (R1,500/month), 2 users max
- vula_growth: 350000 ZAR cents (R3,500/month), 5 users max
- vula_pro: 750000 ZAR cents (R7,500/month), unlimited users

### First 5 target clients (hardcode as seed data for demo)
1. DIGG (Judy Downing, architecture, digg.co.za)
2. Awake SA (Richard Downing, eFoils, awake.co.za)
3. Life Changers Church (community org, Table View)
4. Sporty.TV (media/tech, Century City)
5. Demo Client (for sales demos)

---

## PRIORITY 2: VULA MIND (Local AI Ecosystem)
Build after Vula Business has first paying client.

### What it does
Vula Mind is a privacy-first AI system that runs entirely on the user's
own devices (phone, laptop, desktop) connected in an encrypted mesh
network. No cloud. No API costs. No data leaving the home.

### Core components to build/complete
1. Vula Core (HRM Orchestrator) — ALREADY BUILT in vula/ingestion/
   Extend it with:
   - Routing hints from reflection store
   - Mesh-aware device assignment
   - Soul file (persistent personality config)

2. Vula Mesh (ThinKMesh) — ALREADY BUILT in core/thinkmesh/
   Extend it with:
   - GEPA-style trace-to-improvement loop
   - Mesh transport (mDNS + WebSocket + AES-256-GCM)
   - WhatsApp Business API fallback delivery

3. Vula Mobile (TwoSoul) — SHELL EXISTS at Awehbelekker/TwoSoul-Mobile
   Complete it:
   - Wire API calls to Vula Core backend (port 7437)
   - Implement task dispatch flow
   - Add soul file personality rendering
   - Add mesh status indicator

4. FastAPI server (port 7437) — ALREADY BUILT at vula/api/server.py
   Extend with:
   - /mesh/devices — list available mesh devices
   - /soul — read/update soul file
   - /learn — query reflection store for routing hints

### File structure additions
```
vula-mind/
├── core/
│   ├── hrm/orchestrator.py     ← EXISTS — extend with routing hints
│   ├── thinkmesh/
│   │   ├── graph.py            ← EXISTS — add GEPA loop
│   │   ├── executor.py         ← EXISTS
│   │   ├── merger.py           ← EXISTS
│   │   └── gepa.py             ← NEW — Genetic-Pareto improvement
│   ├── memory/
│   │   ├── reflection.py       ← EXISTS — extend with routing hints query
│   │   └── qdrant_memory.py    ← NEW — semantic long-term memory
│   └── skills/
│       ├── registry.json       ← EXISTS — 8 skills
│       ├── web_scraper.py      ← EXISTS
│       └── vula_business.py    ← NEW — skill that queries Vula Business API
├── mesh/
│   ├── discovery.py            ← EXISTS — mDNS
│   ├── transport.py            ← NEW — WebSocket + encryption
│   └── fallback.py             ← NEW — WhatsApp fallback delivery
├── soul/
│   └── soul.json               ← NEW — persistent identity config
└── api/server.py               ← EXISTS at port 7437 — extend endpoints
```

### Soul file format (soul.json)
```json
{
  "name": "Vula",
  "owner": "Richard",
  "personality": "direct, warm, South African",
  "language_preference": "english",
  "secondary_languages": ["afrikaans"],
  "location": "Cape Town, South Africa",
  "mesh_identity": "primary_desktop",
  "communication_style": "confident, no-fluff, practical"
}
```

---

## PRIORITY 3: VULA WAVE (Watersports Platform)
Build after Vula Mind MVP is deployed.

### What it does
Vula Wave is a marketplace for watersports experiences — book sessions
with instructors/operators at Cape Town beaches, get AI-powered
conditions intelligence, track your rides.

### Tech stack
- Frontend: Next.js 15 (web) + React Native Expo (mobile)
- Backend: FastAPI + PostgreSQL + PostGIS (geospatial)
- AI: GLM-4V for beach camera analysis, DeepSeek for conditions scoring
- Payments: PayFast (ZAR) + Stripe (international)
- Maps: Mapbox (SA beach locations)
- Weather: OpenWeatherMap + Windguru API
- Hosting: Railway

### Core features to build
1. Beach listing with real-time conditions (wind/swell/crowd)
2. Operator onboarding + equipment management
3. Session booking with instant PayFast payment
4. Coach/instructor marketplace
5. Session tracking + leaderboard (Vula Rides gamification)
6. AI beach camera analysis (Vula Eye — YOLO crowd detection)
7. WhatsApp booking confirmations

### Key SA beaches to seed as data
- Big Bay, Bloubergstrand (primary pilot)
- Langebaan
- Muizenberg
- Strand
- Milnerton Lagoon
- Jeffrey's Bay (phase 2)

### Operator pricing
- Free tier: list 1 spot, manual booking
- Pro: R1,200/month — unlimited listings, AI conditions, booking engine
- Transaction fee: 5% per booking (PayFast handles split payments)

---

## PRIORITY 4: VULA TRADE (Autonomous Trading)
Build in parallel, private, for internal capital only.

### What it does
Autonomous AI trading system with self-improvement loop.
Alpaca API for execution. DeepSeek R1 14B for reasoning.
NOT a public product — internal only.

### Architecture (already designed — implement it)
Components:
1. HRM Orchestrator → routes market signals to specialist agents
2. ThinKMesh → runs Bull/Bear/Risk analyst agents in parallel
3. Observation Layer → logs per-trade reasoning traces + outcomes
4. Meta-Agent (Vula Evolve) → reads traces, proposes strategy improvements
5. Evolutionary Archive (Vula Vault) → regime-tagged strategy store
6. Validation Gate → paper trading on Alpaca before any live adoption
7. Live execution → Alpaca API (live account, capped position sizes)

### Key rules (enforce in code, never bypass)
- NEVER self-modify strategies on live capital
- Paper trading validation required: must beat baseline on 3+ regime types
- Max drawdown per session: 2% of portfolio
- Max single position: 5% of portfolio
- Human approval required for structural strategy changes
- All reasoning traces logged to PostgreSQL + TimescaleDB

---

## PRIORITY 5: VULA COMMERCE (White-label E-commerce)
Build alongside Vula Business as a module.

### What it does
Medusa v2 multi-tenant e-commerce platform. Clients of Vula Business
can add a full e-commerce store. Awake SA and Kelp are internal clients.

### Tech stack
- Backend: Medusa v2 (multi-store natively)
- Storefront: Next.js 15 (shadcn/ui design system)
- Payments: PayFast + Stripe
- Hosting: Railway per client (Route 1 — isolated instances)
- Cost per client: ~R500/month (charge R2,000+)

---

────────────────────────────────────────────────────────────────
SECTION 3: SHARED INFRASTRUCTURE TO BUILD FIRST
────────────────────────────────────────────────────────────────

Before any product, build the shared infrastructure layer.
Every product depends on this.

## 1. LiteLLM Gateway (MANDATORY FIRST)
File: infrastructure/litellm/config.yaml

```yaml
model_list:
  - model_name: vula-standard
    litellm_params:
      model: ollama/deepseek-r1:7b
      api_base: http://localhost:11434

  - model_name: vula-advanced
    litellm_params:
      model: ollama/deepseek-r1:14b
      api_base: http://localhost:11434

  - model_name: vula-edge
    litellm_params:
      model: ollama/deepseek-r1:1.5b
      api_base: http://localhost:11434

  - model_name: vula-embed
    litellm_params:
      model: ollama/bge-m3
      api_base: http://localhost:11434

  - model_name: vula-fallback
    litellm_params:
      model: openai/moonshotai/kimi-k2
      api_base: https://openrouter.ai/api/v1
      api_key: os.environ/OPENROUTER_API_KEY

router_settings:
  routing_strategy: least-busy
  fallbacks: [{"vula-standard": ["vula-fallback"]}]

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  database_url: os.environ/DATABASE_URL
```

Start with: `litellm --config config.yaml --port 4000`

## 2. Docker Compose (local dev)
File: docker-compose.yml

```yaml
version: "3.9"
services:
  qdrant:
    image: qdrant/qdrant:latest
    ports: ["6333:6333"]
    volumes: ["./qdrant_data:/qdrant/storage"]

  redis:
    image: redis:7-alpine
    ports: ["6379:6379"]

  n8n:
    image: n8nio/n8n:latest
    ports: ["5678:5678"]
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=vula
      - N8N_BASIC_AUTH_PASSWORD=vula2026
    volumes: ["./n8n_data:/home/node/.n8n"]
```

## 3. Supabase Schema (run in Supabase SQL editor)
File: db/migrations/001_vula_initial.sql

Tables to create:
- vula_tenants (multi-tenant core)
- vula_users (per-tenant users with RBAC)
- vula_documents (uploaded + ingested files)
- vula_chunks (document chunks metadata — embeddings in Qdrant)
- vula_queries (query history per tenant)
- vula_reflections (learning loop store)
- vula_subscriptions (PayFast subscription state)
- vula_scrape_jobs (async scraping queue)

All tables: enable RLS, add tenant_id to every row, create policies.

## 4. Environment Variables Template
File: .env.example

```
# Supabase
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# AI
LITELLM_BASE_URL=http://localhost:4000
LITELLM_API_KEY=
OLLAMA_BASE_URL=http://localhost:11434

# Vector Store
QDRANT_URL=http://localhost:6333

# Payments
PAYFAST_MERCHANT_ID=
PAYFAST_MERCHANT_KEY=
PAYFAST_PASSPHRASE=
STRIPE_SECRET_KEY=

# Notifications
WHATSAPP_TOKEN=
WHATSAPP_PHONE_ID=
RESEND_API_KEY=
TEAM_WHATSAPP=+27820000000

# Trading (Vula Trade only)
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_BASE_URL=https://paper-api.alpaca.markets

# External
OPENROUTER_API_KEY=
MAPBOX_TOKEN=

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
VULA_BUSINESS_API_URL=http://localhost:8000
VULA_MIND_API_URL=http://localhost:7437
VULA_WAVE_API_URL=http://localhost:8001
```

────────────────────────────────────────────────────────────────
SECTION 4: CODE STANDARDS (ENFORCE IN EVERY FILE)
────────────────────────────────────────────────────────────────

## Python (FastAPI backends)
- Python 3.12+
- Type hints everywhere — no untyped functions
- Async-first — every route and service function is async def
- Pydantic v2 models for all request/response schemas
- HTTPException with meaningful status codes and messages
- Logging via Python logging module (not print)
- Environment variables via python-dotenv — never hardcode secrets
- Format: black + ruff
- Tests: pytest + pytest-asyncio

## TypeScript (Next.js frontends)
- TypeScript strict mode — no any types
- Server Components by default, Client Components only when needed
- Tailwind CSS utility classes only (no inline styles)
- shadcn/ui component library
- Zod for all form validation
- React Query (TanStack) for data fetching
- No direct API calls from components — use lib/api.ts functions

## Design System
- Font: Cormorant Garamond (display) + Source Code Pro (mono)
- Primary colour: #2C5545 (forest green)
- Accent colour: #C4861A (amber)
- Background: #F7F4EE (warm cream) for marketing
- Background: #080808 (near-black) for dashboards/apps
- Border radius: 8px (cards), 6px (inputs), 20px (badges)
- No purple gradients. No generic AI aesthetics. Feels South African.

## SA-specific requirements in every product
- All prices displayed in ZAR (R symbol, 2 decimal places)
- All prices stored as integers (ZAR cents, never floats)
- PayFast as primary payment gateway
- POPIA compliance note on all data collection forms
- WhatsApp as the primary notification channel (not email)
- Date format: DD Month YYYY (South African standard)
- Phone numbers: +27 format in DB, display as 0XX XXX XXXX

────────────────────────────────────────────────────────────────
SECTION 5: EXISTING CODE — WHAT'S ALREADY BUILT
────────────────────────────────────────────────────────────────

The following files ALREADY EXIST and should be used/extended,
not rewritten from scratch:

### Vula Mind (local AI) — USE THESE
- vula/ingestion/pipeline.py — full ingestion pipeline (OCR → chunk → embed → Qdrant)
- vula/skills/web_scraper.py — Crawl4AI + ScrapeGraphAI scraper with 4 use cases
- vula/api/server.py — FastAPI server on port 7437
- core/thinkmesh/graph.py — TaskGraph + TaskBranch dataclasses
- core/thinkmesh/executor.py — parallel branch runner
- core/thinkmesh/merger.py — branch merger (vote/synthesize/fastest/best)
- core/hrm/orchestrator.py — HRM routing engine
- core/memory/reflection.py — SQLite reflection + learning store
- core/skills/registry.json — 8 skills defined
- mesh/discovery.py — mDNS mesh device discovery

### TwoSoul Mobile — EXTEND THIS
- GitHub: github.com/Awehbelekker/TwoSoul-Mobile
- Has: React Native + Expo, 6 personality modes, Zustand state
- Missing: API connection to Vula Mind server, task dispatch flow

### Vula Business UI — EXTEND THESE
- The onboarding flow (5-step React app) is built
- The FastAPI onboarding API is built
- Missing: all the dashboard routes, document management, query UI

### What DOES NOT EXIST yet (build fresh)
- LiteLLM gateway config
- Supabase schema (full multi-tenant SQL)
- PayFast billing integration
- Vula Wave (FlowCrew) — start fresh with new name
- Vula Trade (PROMETHEUS) — architecture designed, no production code
- Vula Commerce (Medusa v2) — template not built

────────────────────────────────────────────────────────────────
SECTION 6: BUILD SEQUENCE FOR THIS SESSION
────────────────────────────────────────────────────────────────

Build in this exact order. Complete each step before moving on.
Ask for confirmation before starting the next step.

STEP 1: Infrastructure
  [ ] docker-compose.yml (Qdrant + Redis + n8n)
  [ ] .env.example (all environment variables)
  [ ] infrastructure/litellm/config.yaml
  [ ] db/migrations/001_vula_initial.sql (full Supabase schema)

STEP 2: Vula Business Backend
  [ ] vula-business/backend/main.py
  [ ] vula-business/backend/api/tenants.py
  [ ] vula-business/backend/api/documents.py
  [ ] vula-business/backend/api/query.py
  [ ] vula-business/backend/billing/payfast.py
  [ ] vula-business/backend/notifications/whatsapp.py
  [ ] vula-business/backend/requirements.txt

STEP 3: Vula Business Frontend
  [ ] vula-business/frontend/ (full Next.js app)
  [ ] Landing page with pricing
  [ ] 5-step onboarding (use existing onboarding.jsx as base)
  [ ] Client dashboard with all panels
  [ ] Master admin panel

STEP 4: Wire Vula Mind to Vula Business
  [ ] Add vula_business.py skill to Vula Mind skill registry
  [ ] Extend vula/api/server.py with mesh + soul endpoints
  [ ] Connect TwoSoul Mobile to port 7437

STEP 5: Vula Wave foundations
  [ ] Database schema (beaches, operators, sessions, bookings)
  [ ] Core booking API
  [ ] Conditions intelligence service

────────────────────────────────────────────────────────────────
SECTION 7: GITHUB REPO STRUCTURE
────────────────────────────────────────────────────────────────

Repos to create under github.com/Awehbelekker/:

| Repo Name | Product | Visibility |
|---|---|---|
| vula-business | Vula Business (B2B SaaS) | Private |
| vula-mind | Vula Mind (local AI) | Public (open source) |
| vula-wave | Vula Wave (watersports) | Private |
| vula-trade | Vula Trade (trading) | Private |
| vula-commerce | Vula Commerce (e-commerce) | Private |
| vula-mobile | Vula Mobile (TwoSoul renamed) | Public |
| vula-infrastructure | Shared infra configs | Private |

Retire/archive:
- Universal-AI-Soul-Unlimited → migrated to vula-mind
- TwoSoul-Mobile → migrated to vula-mobile
- CogniCore → migrated to vula-business
- PROMETHEUS → migrated to vula-trade

────────────────────────────────────────────────────────────────
SECTION 8: FIRST DEMO SCRIPT (for selling Vula Business)
────────────────────────────────────────────────────────────────

When Richard demos to an ITEC client:

1. Open vula.co.za on phone
2. "This is Vula — it's an AI that learns YOUR specific business"
3. Log in as DIGG demo account
4. Type: "Draft a quote for a commercial office fitout, 500sqm, full architectural service"
5. Watch Vula Draft produce a real quote in DIGG's format with their rates
6. "Now ask it something about your own business"
7. Client types their question
8. "This is what it can do once it's trained on your documents"
9. "Setup takes one hour. First 30 days free. Want to do it this week?"

────────────────────────────────────────────────────────────────
END OF CLAUDE CODE PROMPT
════════════════════════════════════════════════════════════════
```

---

# PART 5: IMMEDIATE ACTION LIST

## This Week (Richard)
1. Test the WhatsApp message from the sales one-pager on 3 ITEC contacts
2. Check vula.co.za and vula.ai domain availability + register
3. Open the Claude Code prompt above in a new Claude Code session
4. Start with STEP 1: docker-compose.yml + .env.example

## This Week (Judy)
1. Set up DIGG as the first Vula Business demo client (uploads 3 DIGG quotes)
2. Test the Vula dashboard with real DIGG documents
3. Identify 2 DIGG clients who could benefit → hand to Richard

## Month 1 Goal
- Vula Business live at vula.co.za
- DIGG as paying client #1 (R1,500/month, Starter)
- Awake SA as client #2 (internal, proves retail use case)
- 1 external ITEC contact signed (R3,500/month, Growth)
- First R6,500 MRR

## Month 3 Goal
- 10 paying clients across tiers
- Vula Mobile connected to Vula Mind
- Vula Wave Big Bay pilot live
- R45,000/month MRR

---

*Vula Group (Pty) Ltd — Cape Town — Unlock everything.*
